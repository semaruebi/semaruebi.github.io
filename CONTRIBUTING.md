# 開発ガイド 💉

ウチがこのプロジェクトの開発フローを説明するわね！

## 🌿 ブランチ戦略

### ブランチ構成

```
main          - 本番環境（GitHub Pagesへ自動デプロイ）
└─ develop    - 開発統合ブランチ
   └─ feature/* - 機能開発ブランチ
```

### ブランチ命名規則

- `feature/機能名` - 新機能追加
- `fix/修正内容` - バグ修正
- `docs/変更内容` - ドキュメント更新
- `refactor/対象` - リファクタリング
- `style/対象` - スタイル調整
- `test/対象` - テスト追加

**例：**
```bash
feature/elite-enemy-tags
fix/drag-scroll-issue
docs/update-readme
refactor/card-component
style/improve-dark-theme
test/add-validation-tests
```

## 🔄 開発フロー

### 1. 新しい機能開発を始める

```bash
# 最新のmainブランチから開始
git checkout main
git pull origin main

# featureブランチを作成
git checkout -b feature/your-feature-name

# または developブランチから
git checkout develop
git pull origin develop
git checkout -b feature/your-feature-name
```

### 2. 開発とコミット

```bash
# 変更をステージング
git add .

# コンベンショナルコミット形式でコミット
git commit -m "feat: 新機能の説明"
# または
git commit -m "fix: バグ修正の説明"
```

**コミットメッセージのタイプ：**
- `feat:` - 新機能
- `fix:` - バグ修正
- `docs:` - ドキュメント変更
- `style:` - コードの意味に影響しないスタイル変更
- `refactor:` - リファクタリング
- `test:` - テスト追加・修正
- `chore:` - ビルド・ツール関連

### 3. リモートにプッシュ

```bash
# featureブランチをプッシュ
git push origin feature/your-feature-name
```

これでGitHub Actionsが自動的にテストを実行するわ！

### 4. プルリクエスト作成

1. GitHubでプルリクエストを作成
2. ベースブランチ：`develop` または `main`
3. タイトル：わかりやすく簡潔に
4. 説明：変更内容、理由、テスト方法を記載

**PRテンプレート例：**
```markdown
## 変更内容
- 精鋭タグの画像表示機能を追加したのよ

## 理由
- ユーザーが視覚的に精鋭を選択できるようにするため

## テスト方法
1. 精鋭選択ボタンをクリック
2. モーダルが開くことを確認
3. 精鋭を選択して投稿
4. 画像が表示されることを確認

## スクリーンショット
（あれば貼り付け）
```

### 5. コードレビュー＆マージ

1. CIテストがすべてパスすることを確認
2. レビュー後、マージ
3. マージ後はfeatureブランチを削除

```bash
# ローカルブランチ削除
git branch -d feature/your-feature-name

# リモートブランチ削除（GitHubでも可）
git push origin --delete feature/your-feature-name
```

### 6. mainへの反映（リリース）

```bash
# developからmainへのPRを作成
git checkout main
git pull origin main
git merge develop
git push origin main
```

mainへのpushで自動的にGitHub Pagesへデプロイされるわ！

## 🧪 テスト

### ローカルでテスト実行

```bash
# 依存関係をインストール
npm install

# すべてのテストを実行
npm test

# 個別実行
npm run lint:js        # JavaScript リント
npm run lint:css       # CSS リント
npm run validate:html  # HTML バリデーション
npm run check:images   # 画像存在確認
```

### CI/CDパイプライン

GitHub Actionsが以下を自動実行するわ：

1. **プッシュ時**（main/develop/feature/*）
   - JavaScript リント
   - CSS リント
   - HTML バリデーション
   - 画像存在確認

2. **mainブランチへのマージ時**
   - 上記すべて＋GitHub Pagesへ自動デプロイ

## 📝 コーディング規約

### JavaScript
- **セミコロン**: 必須
- **引用符**: シングルクォート `'`
- **インデント**: スペース2個
- **命名**: キャメルケース（関数・変数）
- **コメント**: 重要な処理には必ず説明を

### CSS
- **命名**: ケバブケース（`.card-container`）
- **インデント**: スペース2個
- **順序**: レイアウト → ボックスモデル → タイポグラフィ → 装飾

### HTML
- **属性順序**: `class` → `id` → その他 → イベント
- **インデント**: スペース2個
- **セマンティック**: 適切なHTML5タグを使用

## 🔧 便利なコマンド

```bash
# 精鋭画像リストを更新
npm run update:elite-images

# または Windows
scripts\update-elite-images.bat

# 全テスト実行
npm test

# package.json の依存関係を更新
npm update

# セキュリティ監査
npm audit
```

## 🚨 注意事項

### やってはいけないこと ❌

1. **mainブランチへの直接コミット**
   ```bash
   # ダメな例
   git checkout main
   git commit -m "fix: 直接修正"
   ```

2. **force pushを本番ブランチで使う**
   ```bash
   # 絶対にダメ！
   git push --force origin main
   ```

3. **パスワードやAPIキーをコミット**
   - 環境変数を使うのよ
   - `.gitignore`で除外

4. **コミットメッセージが曖昧**
   ```bash
   # ダメな例
   git commit -m "update"
   git commit -m "fix bug"
   
   # 良い例
   git commit -m "feat: 精鋭タグ画像表示機能を追加"
   git commit -m "fix: ドラッグスクロールが動作しない問題を修正"
   ```

### やるべきこと ✅

1. **小さくて頻繁なコミット**
   - 1つの機能や修正ごとにコミット

2. **わかりやすいブランチ名**
   - `feature/add-elite-enemy-search`
   - `fix/modal-z-index-issue`

3. **テストを実行してからプッシュ**
   ```bash
   npm test
   git push
   ```

4. **定期的にmain/developと同期**
   ```bash
   git checkout feature/your-feature
   git fetch origin
   git rebase origin/main
   ```

## 🐛 トラブルシューティング

### コンフリクトが発生したら

```bash
# 1. 最新のmainを取得
git fetch origin main

# 2. リベース
git rebase origin/main

# 3. コンフリクトを手動解決
# （エディタでファイルを編集）

# 4. 解決後
git add .
git rebase --continue

# 5. プッシュ（初回はforce必要）
git push --force-with-lease origin feature/your-feature
```

### CIテストが失敗したら

1. ローカルで該当テストを実行
   ```bash
   npm run lint:js
   npm run lint:css
   ```

2. エラーを修正

3. 再度コミット＆プッシュ
   ```bash
   git add .
   git commit -m "fix: リンターエラーを修正"
   git push
   ```

## 📚 参考リンク

- [Conventional Commits](https://www.conventionalcommits.org/)
- [GitHub Flow](https://docs.github.com/ja/get-started/quickstart/github-flow)
- [Semantic Versioning](https://semver.org/lang/ja/)

---

質問があったらウチに聞いてちょうだい💉

